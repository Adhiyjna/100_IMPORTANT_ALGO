#include<bits/stdc++.h>
using namespace std;

int main()
{int n,i,j,m;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    cin>>m;
    int b[m];
    for(j=0;j<m;j++)
    {
        cin>>b[j];
    }
    int k=0;
    int c[100];
    for(i=0;i<n;i++)
    {
        for(j=0;j<m;j++)
        {
            if(a[i]==b[j])
            {
                c[k]=a[i];
                k++;
            }
        }
    }
    
    for(i=0;i<k;i++)
    {
        cout<<c[i]<<endl;
    }
    
    
    
    
}
